use crate::DeviceBuffer;
use hid_and_seek::HumanInterfaceDevice;

pub fn transcode_device(
    config: &HumanInterfaceDevice,
    buf_new: Option<&DeviceBuffer>,
    buf_last: Option<&DeviceBuffer>,
) -> () {
    
}
